@page "/expense"
@inherits ExpenseBase

<h3>Expense</h3>

@if (Expenses == null)
{
    <div class ="d-flex justify-content-center">
        <div class = "spinner-border"></div>
    </div>
}
else
{
    @if (Expenses.Any())
    {
        <table class="table table-striped table-bordered my-3">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Date</th>
                    <th>Name</th>
                    <th>Sum</th>
                    <th>Comment</th>
                    <th>ExpenseTypeId</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Expenses)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Date.ToShortDateString()</td>
                        <td>@item.ExpenseType.Name</td>
                        <td>@item.Sum</td>
                        <td>@item.Comment</td>
                        <td>@item.ExpenseTypeId</td>
                        <td>
                            <a href = "@($"expenseedit/{item.Id}")" class = "btn btn-outline-primary m-1">Edit</a>
                            <a href = "#" class = "btn btn-outline-primary m-1">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
}
